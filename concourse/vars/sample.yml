# Sample Concourse vars for backrest-operator pipeline validation.
# kubeconfig-prq lives in KCM namespace concourse (not in this file).

github-access-token:
  username: git
  password: CHANGE_ME

ghcr-credentials:
  username: CHANGE_ME
  password: CHANGE_ME

webhook-token-backrest-operator: CHANGE_ME
webhook-token-backrest-operator-tags: CHANGE_ME

slack-notify:
  secret: CHANGE_ME
